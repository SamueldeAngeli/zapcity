window.ZAPCITY_CONFIG = {
  // Deixe vazio para usar a mesma origem. No Vercel puro, o site usa modo demo.
  // Quando a API estiver na VPS, use algo como: https://api.seudominio.com
  apiBaseUrl: '',
};
